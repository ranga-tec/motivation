@using Poms.Web.ViewModels
@using Microsoft.AspNetCore.Mvc.Rendering
@{
    ViewData["Title"] = "Dashboard";
}

<h1 class="mb-4">Exceed Lanka Prosthetic &amp; Orthotic Data Management System</h1>

<div class="row">
    <div class="col-md-3">
        <a asp-controller="Patients" asp-action="Index" class="text-decoration-none">
            <div class="card text-white bg-primary mb-3">
                <div class="card-body">
                    <h5 class="card-title">Total Patients</h5>
                    <h2 class="card-text">@ViewBag.TotalPatients</h2>
                </div>
            </div>
        </a>
    </div>
    <div class="col-md-3">
        <a asp-controller="Home" asp-action="ActiveRecordsDrillDown" class="text-decoration-none">
            <div class="card text-white bg-success mb-3">
                <div class="card-body">
                    <h5 class="card-title">Active Records</h5>
                    <h2 class="card-text">@ViewBag.ActiveRecords</h2>
                </div>
            </div>
        </a>
    </div>
    <div class="col-md-3">
        <div class="card text-white bg-secondary mb-3">
            <div class="card-body">
                <h5 class="card-title">Active Patients</h5>
                <h2 class="card-text">@ViewBag.ActivePatients</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-white bg-info mb-3">
            <div class="card-body">
                <h5 class="card-title">Today's Activities</h5>
                <p class="mb-0">Assessments: @ViewBag.TodaysAssessments</p>
                <p class="mb-0">Fittings: @ViewBag.TodaysFittings</p>
                <p class="mb-0">Deliveries: @ViewBag.TodaysDeliveries</p>
            </div>
        </div>
    </div>
</div>

<div class="card mb-4">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Appointments / Date-Based Actions</h5>
        <div>
            <a asp-controller="Appointments" asp-action="Index" class="btn btn-sm btn-outline-primary">View Appointments</a>
            <a asp-controller="Appointments" asp-action="Create" class="btn btn-sm btn-primary">+ New Appointment</a>
        </div>
    </div>
    <div class="card-body">
        @{
            var todaysAppointments = ViewBag.TodaysAppointments as IEnumerable<Poms.Domain.Entities.Appointment>;
        }
        @if (todaysAppointments != null && todaysAppointments.Any())
        {
            <table class="table table-sm">
                <thead><tr><th>Time</th><th>Patient</th><th>Type</th></tr></thead>
                <tbody>
                    @foreach (var a in todaysAppointments)
                    {
                        <tr><td>@a.AppointmentTime</td><td>@a.Patient.PatientNumber - @a.Patient.FullName</td><td>@a.Type</td></tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p class="text-muted mb-0">No appointments scheduled for today.</p>
        }
    </div>
</div>

<div class="row mt-2">
    <div class="col-md-6">
        <div class="card mb-3">
            <div class="card-header"><h5 class="mb-0">Quick Actions</h5></div>
            <div class="card-body">
                <a asp-controller="Patients" asp-action="Create" class="btn btn-primary mb-2 d-block">Register New Patient</a>
                <a asp-controller="Episodes" asp-action="Create" class="btn btn-success mb-2 d-block">Create New Record / Assessment</a>
                <a asp-controller="Appointments" asp-action="Create" class="btn btn-info mb-2 d-block">Create New Appointment</a>

                @{
                    var quickActionPatients = ViewBag.Patients as SelectList;
                }
                <form asp-controller="Fittings" asp-action="QuickAdd" method="get" class="input-group mb-2">
                    <select name="patientId" class="form-select" required>
                        <option value="">Add Fitting for…</option>
                        @foreach (var p in quickActionPatients!) { <option value="@p.Value">@p.Text</option> }
                    </select>
                    <button type="submit" class="btn btn-outline-success">Go</button>
                </form>
                <form asp-controller="Deliveries" asp-action="QuickAdd" method="get" class="input-group mb-2">
                    <select name="patientId" class="form-select" required>
                        <option value="">Add Delivery for…</option>
                        @foreach (var p in quickActionPatients!) { <option value="@p.Value">@p.Text</option> }
                    </select>
                    <button type="submit" class="btn btn-outline-info">Go</button>
                </form>
                <form asp-controller="FollowUps" asp-action="QuickAdd" method="get" class="input-group mb-2">
                    <select name="patientId" class="form-select" required>
                        <option value="">Add Follow-up for…</option>
                        @foreach (var p in quickActionPatients!) { <option value="@p.Value">@p.Text</option> }
                    </select>
                    <button type="submit" class="btn btn-outline-warning">Go</button>
                </form>
            </div>
        </div>
    </div>
    <div class="col-md-6">
        <div class="card mb-3">
            <div class="card-header"><h5 class="mb-0">Recent Activities</h5></div>
            <div class="card-body" style="max-height: 360px; overflow-y: auto;">
                @{
                    var recent = ViewBag.RecentActivities as List<RecentActivityItem>;
                }
                @if (recent != null && recent.Any())
                {
                    <table class="table table-sm">
                        <thead><tr><th>Date</th><th>Patient</th><th>Activity</th><th>User</th><th>Location</th></tr></thead>
                        <tbody>
                            @foreach (var item in recent)
                            {
                                <tr>
                                    <td>@item.Date.ToString("dd-MMM HH:mm")</td>
                                    <td>@item.PatientNumber<br /><small class="text-muted">@item.PatientName</small></td>
                                    <td>@item.ActivityType</td>
                                    <td>@item.User</td>
                                    <td>@item.Location</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
                else
                {
                    <p class="text-muted mb-0">No recent activity.</p>
                }
            </div>
        </div>
    </div>
</div>
